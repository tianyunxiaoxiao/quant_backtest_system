# 指数内多头因子回测系统：从零构建、审计、优化到最终验收的完整技术手册

> 文档日期：2026-08-07  
> 项目：`qbt` 指数内多头因子回测框架  
> 当前项目位置：`/Users/a1/Desktop/添橙/TC5days20260806/quant_backtest_system`  
> 历史开发位置：`/Users/a1/Desktop/添橙/5days20260806/quant_backtest_system`  

## 1. 文档目的

本文完整记录该项目从原始数据和导师规范出发，经过架构设计、数据摄取、回测引擎、分析、报告、测试、独立审计、缺陷修复、安全加固、真实数据重跑和最终验收的全过程。

本文不是简单的功能清单。它特别保留了以下内容：

1. 最初为什么采用当前架构和口径。
2. 每个模块如何逐步建立并用数据验证。
3. 第一版虽然测试通过，为什么仍被独立审计判定为不可接受。
4. 每个严重缺陷的数值证据、根因、修复方法和回归测试。
5. Bugbot 与 Security Review 提出的完整问题类别及其关闭方式。
6. 最终两个全窗口真实数据验收运行的结果、哈希和人工复算证据。
7. 当前仍然存在的数据边界和 2026-08-07 项目目录改名后的可移植性注意事项。

最终结论是：在导师批准的 v1 和“管道正确性验收”范围内，最终版本达到 20/20 条完成条件；但它不能被描述为完全无偏、可直接用于生产投资结论的策略研究，因为官方基准、历史退市/ST、公司行动、PIT 行业与机构级风格数据仍不完整。

## 2. 权威依据与验收边界

### 2.1 权威文档

开发和最终审计按以下优先级解释需求：

1. `19_portfolio_backtest_framework.md`：导师给出的正式实施规范。
2. `待导师确认事项清单_副本.md`：针对数据、执行、选股、成本、统计和交付边界的确认结果。
3. 其他 Markdown：仅作为历史上下文，不覆盖导师文档。

### 2.2 v1 的系统职责

系统只接收一个已经在外部构造完成的最终 `FactorFrame`，不执行多因子标准化、合成、权重学习、符号反转或事后因子筛选。系统负责：

- 使用指定指数的 point-in-time 成分股构建股票池。
- 在信号日选择最终因子排名前 30% 的股票。
- 生成目标权重、订单和成交。
- 模拟实际持仓、现金、交易约束和交易成本。
- 从实际成交后的账户状态计算收益，而非用目标权重乘未来收益。
- 构建组合、基准和超额净值。
- 输出绩效、Alpha/Beta、风格、选股、流动性和约束诊断。
- 登记数据、因子、配置、代码、结果和产物哈希。
- 让报告器只消费冻结结果，不修改或重新推导业务结果。

### 2.3 最终冻结的主要口径

- 验收指数：中证 500，`000905.SH`。
- 配置窗口：`2018-01-01` 至 `2026-03-31`。
- 实际有效起点：`2019-01-02`，因为首个 PIT 指数快照为 `2019-01-01`，禁止向前回填。
- 样本外起点：`2023-01-01`。
- 默认调仓：日频；周频和月频可配置。
- 信号时间：T 日收盘后。
- 成交时间：T+1。
- 默认成交价：`adj_vwap`，即 T+1 全天 VWAP；`adj_open` 和 `adj_close` 仅作敏感性对照。
- 选股分母：PIT 指数成分、T 日可交易、因子有效三者交集。
- 选股比例：向上取整的前 30%。
- 并列处理：`asset_id` 升序，确保确定性。
- 默认权重：因子强度权重，使用第一只落选股票的因子值作为 cutoff。
- 单票目标上限：5%。
- 现金缓冲：1%，只应用一次。
- 买入：主板 100 股整手，科创板 200 股整手。
- 卖出：允许零股清仓。
- 执行顺序：先卖后买，卖出回款当日可用。
- 未成交：当日收盘取消，并保存明确原因。
- 最大 ADV 参与率：10%，缺失 ADV 时拒绝成交。
- 涨跌停缓冲：距离法定涨跌停 0.5 个百分点即按不可交易处理。
- 初始资金：100,000,000。
- 无风险利率：0。
- 年化交易日：252。
- 回归：OLS 与 Newey-West 同时报告，默认 NW lag 为 5。

## 3. 最终工程结构

```text
quant_backtest_system/
  src/qbt/
    contracts/      请求、配置、输入帧、记录和冻结结果契约
    data/           行情与指数摄取、PIT 面板、交易日历、可交易性、基准、风格、哈希
    engine/         选股、权重、成本、执行、主回测编排
    analytics/      收益、回撤、绩效、Alpha/Beta、风格、选股诊断
    reporting/      Parquet/JSON、图表、Markdown、完整加载与一致性验证
    factors/        五个固定方向的演示因子
    cli/            命令行入口
  tests/
    unit/           单元和对抗性测试
    regression/     手算账户与哈希回归测试
    integration/    真实数据、报告不变性和完整重载测试
  warehouse/        分年 Parquet 行情仓库、指数数据和交易日历缓存
  artifacts/        每次运行的完整产物
  verification/     最终 20 条导师合规审计
  scripts/          数据摄取入口
```

当前 `src`、`tests` 和 `scripts` 中约有 9,535 行 Python/工程代码。最终审计时统计的包源文件为 36 个、源代码约 7,718 行。

## 4. 从零开始的开发过程

### 4.1 阶段 0：先审规范和数据，而不是直接写策略

项目首先逐行阅读导师规范和确认清单，然后盘点工作目录中的原始数据：

- 5,495 个股票 CSV 文件。
- 中证 300、中证 500、中证 1000 和中证 2000 的月度成分文件。
- 股票文件包含开高低收、均价、成交量、成交额、流通市值、总市值、换手率、PE、PB、PS、PCF 等字段。
- 中证 500 文件包含 76 个有效月度快照，每个快照 500 只股票，共 38,000 条有效记录。
- 中证 500 快照在 `2020-09` 至 `2021-04`、`2023-01` 至 `2023-04` 共缺 12 个月。
- 37 个快照日期落在非交易日，后续必须映射到其后的首个交易日生效。
- 46 只历史成分股不在行情库中，早期单期权重缺口最高 5.73%，构成已知幸存者偏差。

这一阶段确定了最关键的研究纪律：不使用最新成分回填历史；缺少官方数据时显式降级并披露，而不是伪造完整性。

### 4.2 阶段 1：建立项目骨架与强类型契约

最先实现 `contracts` 层，将系统中的不同业务对象分开：

- `FactorFrame`：最终因子及其方向、版本和内容哈希。
- `MarketPriceFrame`：原始/复权价格、成交量和复权因子。
- `TradabilityFrame`：停牌、涨停、跌停、允许买入、允许卖出。
- `PortfolioLiquidityData`：ADV 等流动性数据。
- `ResolvedLongOnlyBacktestData`：DataPortal 完成 PIT 对齐后的冻结输入。
- `OrderRecord` 与 `FillRecord`：订单和成交事件。
- `LongOnlyFactorBacktestResult`：引擎输出的完整冻结结果。
- `RunManifest`、`DatasetRef`、`RunArtifact`：运行、数据和产物血缘。

配置被拆为 `ClockConfig`、`SelectionConfig`、`WeightingConfig`、`ConstraintConfig`、`CostConfig`、`ExecutionConfig`、`RegressionConfig` 和总配置。后续优化进一步加入有限数、范围、互斥口径和不支持能力的显式校验，避免配置被记录但没有真正执行。

### 4.3 阶段 2：行情摄取与复权价格重建

`data/ingest_prices.py` 完成如下工作：

1. 读取 GB18030/CSV 数据并标准化中文列名。
2. 把 `N/A` 等异常值转换为缺失。
3. 统一资产代码和日期类型。
4. 依据原始收盘价与可用调整信息构建阶梯化复权因子。
5. 生成原始与复权 OHLC/VWAP。
6. 校验 OHLC 关系、日期范围、重复键和数量字段。
7. 按年写入 Parquet 分区。

最终仓库统计：

- 输入文件：5,495。
- 成功文件：5,494。
- 失败文件：1；`920011.BJ` 的唯一超范围行按 G2 截断规则处理。
- 总行数：10,638,434。
- 资产数：5,494。
- 日期范围：`2015-06-01` 至 `2026-04-07`。
- 分区：2015 至 2026 共 12 个年度 Parquet。
- OHLC 关系违规：0 行。
- 零成交量行：201,695，保留用于停牌识别。

早期版本会在新数据完全校验前删除或替换现有仓库。安全审计后改为事务式摄取：先写 staging，验证完整数据和分区，再原子交换；交换失败时恢复最新完整备份；单文件解析失败必须报告具体文件，不能静默吞掉。

### 4.4 阶段 3：指数成分、PIT 展开和基准

`data/ingest_index.py` 对不同 Excel 文件的列名差异采用后缀/语义解析，而非依赖一个硬编码列名。摄取时执行：

- 指数 ID 白名单式格式约束，阻止路径穿越和危险标识符。
- 日期、成分代码和权重有限性校验。
- 权重非负校验。
- 每个快照合计权重的合理区间校验。
- 去除 Wind 文件末尾的“数据来源”行。

月度快照展开为日度 PIT 成分和权重时，只向未来填充至下一个快照，不向过去回填。

官方全收益指数点位尚不可用，因此 `data/benchmark.py` 使用 PIT 月度权重和复权股票收益合成基准。月内按 buy-and-hold 漂移；下一快照生效时重置权重。初版只在快照日期本身是交易日时重置，导致 37 个非交易日快照可能失效。最终逻辑使用交易日历 `searchsorted(..., side="left")`，将快照映射到其后首个交易日。

用于证明修复的合成测试：新快照把权重切换到上涨 10% 的股票后，第二日基准应为 5.5%；错误版本给出 1.0%，修复后精确得到 5.5%。

### 4.5 阶段 4：交易日历、行情面板和可交易性

`data/panel.py` 以市场活跃股票数量构造交易日历，再将大表裁剪为回测所需日期和资产的宽矩阵，避免全市场全字段长期驻留内存。

`data/tradability.py` 实现：

- 主板、创业板、科创板、北交所等板块的历史涨跌停比例。
- 新股上市初期规则入口。
- T 日价格距离涨跌停 0.5 个百分点的缓冲判定。
- 零成交量行停牌。
- 上市区间内缺行停牌。
- 买入与卖出方向分别生成允许矩阵。

开发中曾错误地用“开盘是否触及涨停”判断 VWAP 订单是否可成交。真实市场中股票可能开盘触板后回落，全天 VWAP 仍可成交。最终可交易性判定与配置的成交价字段保持一致，执行级 `fill_price_field` 覆盖会同步驱动价格元数据和可交易性。

交易日历缓存后来增加四层验证：schema 版本、缓存文件自身哈希、源分区内容指纹和构造配置。即便攻击者恢复文件时间和大小，源内容改变仍会导致缓存失效。

### 4.6 阶段 5：风格代理和 DataPortal

由于没有 Barra 和 PIT 行业/财务分类，`data/style.py` 构造五个可由现有数据支持的截面代理：

- Size：`ln(流通市值)`。
- Value：`1 / PB`。
- Momentum：过去 252 日收益剔除最近 21 日。
- Volatility：历史日收益波动。
- Liquidity：换手/成交活跃度代理。

每个交易日做截面标准化和极值裁剪。Growth、Quality、Leverage 明确标记为缺失，不用不可靠字段填补。

`PortfolioDataPortal` 将行情、指数、交易日历、可交易性、ADV、风格和合成基准统一对齐到同一日期、资产和复权空间。它还负责：

- 解析实际有效窗口。
- 拒绝非法窗口。
- 生成日/周/月调仓信号日。
- 删除末端无法完成“信号 -> T+1 成交 -> 估值”链路的信号日。
- 登记每个输入数据集的行列数、日期范围、URI、哈希和说明。
- 披露缺失 PIT 月份、历史成分行情缺口和有效起点变化。

### 4.7 阶段 6：选股逻辑

`engine/selection.py` 按每个调仓日执行：

1. 取 PIT 指数成分。
2. 与信号日可交易股票相交。
3. 与因子有限值相交。
4. 根据 `direction` 把“更优”统一为降序排名。
5. 选择 `ceil(eligible_count * 30%)`。
6. 因子值并列时按 `asset_id` 升序。
7. 保存成分数、有效数、入选数和每类排除原因。

这一层不查看未来收益是否为空，也不修改因子方向。五个演示因子的方向在元数据中预先写死，防止跑完后根据结果翻转符号。

### 4.8 阶段 7：权重构造与约束

`engine/weighting.py` 支持：

- `factor_strength`：默认。
- `equal_weight`：等权。
- `index_weight`：入选股票的指数权重重新归一化。

因子强度权重的核心是：以第一只落选股票的得分作为 cutoff，入选股票的正强度为 `selected_score - cutoff`。这样每只入选股票都有正权重。如果全部并列导致总强度为零，则按配置退回等权。

单票 5% 目标上限通过迭代重新分配实现；总可投资预算为 `1 - cash_buffer`。初版现金缓冲在目标权重和执行层各应用一次，形成双重扣减。最终只在目标预算中应用一次。

`max_turnover` 后来实现双层保证：

1. 目标层按前一期目标到新目标的单边换手缩放过渡。
2. 执行层根据实际成交名义金额设置每次调仓预算，防止价格变动和滑点使实际换手越界。

### 4.9 阶段 8：成本模型

`engine/costs.py` 把显式现金费用和经济成本分开：

- 佣金：双边 0.025%。
- 过户费：双边，2022-04-29 前 0.002%，之后 0.001%。
- 印花税：仅卖出，2023-08-28 前 0.1%，之后 0.05%。
- 滑点：默认 12 bps，嵌入成交价格。
- 冲击：v1 作为嵌入滑点的模型记录。

`FillCosts.explicit_total` 只包含佣金、印花税和过户费，是真正从现金扣除的费用；`FillCosts.total` 还包含滑点和冲击，用于费前/费后归因。

这个拆分修复了第一版最严重的问题之一：滑点已经使成交价变差，却又作为现金费用扣一次。对一个 10,000 账户的 1% 滑点合成交易，正确期末 NAV 为 9,910；错误版本为 9,820；修复后回到 9,910。

### 4.10 阶段 9：执行与账户状态

`engine/execution.py` 是系统核心。每个交易日维护：

- 原始股数与复权股数。
- 持仓成本基础。
- 持仓市值。
- 已实现和未实现 PnL。
- 可用现金与冻结现金。
- 订单、成交和未成交原因。
- 当日买卖金额、显式费用和经济成本。
- 实际权重和单边换手。
- 独立账户恒等式残差。

调仓日顺序：

1. 用信号日原始收盘价和账户权益计算目标原始股数，避免使用 T+1 成交价进行前视 sizing。
2. 先处理减仓、清仓和指数退出。
3. 卖出允许零股，且回款当日可用于买入。
4. 再处理买入，按市场整手向下取整。
5. 同时受可交易性、现金、ADV 和实际换手预算约束。
6. 未成交数量记录金额与原因，当日收盘取消。
7. 用成交后实际持仓估值，生成净值和收益。

复权数量换算曾是初版致命错误。最终采用一致的“原始价格/原始股数”和“复权价格/复权股数”等值关系，使公司行动前后不会产生虚假净值跳变。对一个平价、非 1 复权因子的 10,000 账户，错误版本立即变成 3,250，修复后保持 10,000。

账户恒等式不是用残差倒推 PnL，而是独立按持有、卖出和买入三个段计算毛 PnL：

```text
close_net_assets
= open_equity
 + holding_mark_to_market_pnl
 + sell_segment_pnl
 + buy_segment_pnl
 - all_economic_costs
```

现金则独立满足：

```text
close_cash
= open_cash
 + sell_notional
 - buy_notional
 - commission
 - stamp_duty
 - transfer_fee
```

滑点不再从现金重复扣除，因为它已经体现在 `sell_notional` 或 `buy_notional` 的成交价格中。

### 4.11 阶段 10：演示因子

最终提供五个固定定义的因子：

1. `reversal_20d`：过去 20 个交易日复权收益，`direction=-1`。
2. `bp_1_over_pb`：`BP = 1 / PB`，只接受正且有限的 PB，`direction=+1`。
3. `size_ln_float_mktcap`：流通市值对数，`direction=-1`。
4. `turnover_21d`：过去 21 日平均换手，`direction=-1`。
5. `volatility_252d`：过去 252 日收益波动，`direction=-1`。

滚动因子最少需要约 80% 窗口数据，避免新上市股票仅凭极少观测产生极端值。每个因子都通过未来截断不变性测试：删除未来行后，历史因子值必须完全不变。

BP 是审计后补齐的导师批准因子。其 PB 是否完全 PIT 仍取决于数据商是否对历史估值字段重述，因此元数据明确披露。

### 4.12 阶段 11：收益、绩效、回归和风格分析

`analytics/returns.py` 保存：

- 组合净收益和毛收益。
- 基准收益。
- 算术日超额。
- 组合、毛组合、基准和几何超额净值。
- 三类回撤序列和回撤区间表。

几何超额净值最终定义为：

```text
excess_nav = portfolio_nav / benchmark_nav
```

第一版错误地复利 `portfolio_return - benchmark_return`。示例中错误值为 1.05，正确比值为 1.047619。修复还同步覆盖了全样本、IS、OOS 中的超额最大回撤，避免局部统计继续使用旧公式。

`analytics/metrics.py` 计算：

- 累计和年化收益。
- 年化波动、Sharpe、Sortino、Calmar。
- 最大回撤、峰值、谷底、修复日和持续期。
- 几何与算术超额。
- 跟踪误差和信息比率。
- 日、月、年胜率。
- 单边换手、总成本和成本侵蚀。
- 平均持股数、前十集中度、现金比率和 HHI。
- 年度、月度和滚动统计。

成本侵蚀曾以组合毛收益为分母，最终改为相对基准的毛超额口径，避免把 beta 收益误认为可被成本侵蚀的 alpha。

`analytics/alphabeta.py` 直接实现 OLS 和 Newey-West 协方差，不依赖 statsmodels。输出日/年化 Alpha、Beta、OLS/NW 标准误、t 值、p 值、R²、残差波动、滚动回归和 Alpha/Beta 累计贡献。合成数据测试能够精确恢复 `alpha=0.0002`、`beta=1.5` 和 `R²=1`。

`analytics/style.py` 同时计算组合、指数和主动暴露，并强制：

```text
active_exposure = portfolio_exposure - index_exposure
```

还计算主动暴露与当日/滞后超额收益的关系，并对全缺失或零方差风格进行无警告处理。

### 4.13 阶段 12：选股诊断

`analytics/selection_report.py` 用无摩擦模拟区分：

- 等权选股信号表现。
- 目标权重信号表现。
- 实际可实现费后表现。
- 实施损耗。

初版诊断在周频/月频策略下仍隐式每日再平衡，并使用收盘到收盘收益，与真实 VWAP 执行不一致。最终改为持仓数量模拟：只在映射后的真实调仓成交日交易，成交日计入 VWAP 到收盘收益，非调仓日保持数量不变并自然漂移。

实施损耗定义为：

```text
implementation_shortfall
= target_weight_diagnostic_return - actual_net_return
```

它涵盖交易成本、整手、涨跌停、停牌、现金、ADV 和未成交带来的差异。

### 4.14 阶段 13：主回测器编排

`LongOnlyFactorBacktester` 按固定顺序连接：

```text
Request
  -> DataPortal.resolve
  -> selection
  -> target weighting
  -> target turnover scaling
  -> execution
  -> return/NAV construction
  -> full/IS/OOS performance
  -> Alpha/Beta
  -> style exposure
  -> selection diagnostics
  -> constraint reports
  -> diagnostics
  -> complete result hash
  -> frozen LongOnlyFactorBacktestResult
```

引擎不能导入 `qbt.reporting` 或 Matplotlib。该边界由独立子进程测试和最终源代码扫描共同保证。

持仓产物最终使用两级列索引，完整保存：`quantity_raw`、`quantity_adjusted`、`cost_basis`、`market_value`、`unrealized_pnl` 和 `weight`。初版只保存市值，无法审计数量和成本基础，后续已补全。

换手最终来自实际成交名义金额，而非权重变化。这样非调仓日单纯价格漂移不会被错误计为交易。

### 4.15 阶段 14：报告、产物和 CLI

`LongOnlyFactorReporter` 只接收冻结结果。它生成：

- 事件和时序 Parquet。
- 绩效、回归、风格、选股、约束和诊断 JSON。
- 完整 Markdown 报告。
- 14 张图表。
- `run_manifest.json`。

14 张图包括净值对比、超额净值、回撤、月度收益、年度收益、样本指标、滚动指标、Alpha/Beta 贡献、滚动 Alpha/Beta、风格时序、风格热力图、指数覆盖、换手与成本、未成交与 ADV。

早期图表存在中文方框、ADV/换手信息不完整和不可用风格空图例。最终加入 macOS/Linux CJK 字体回退、非空像素检查、人工视觉检查，并在绘图前过滤全空风格，避免暗示不存在的数据。

报告器支持 PNG/SVG/PDF、DPI、尺寸、表格行数以及是否写 Markdown/JSON。安全审计后，所有报告先写入验证过的 staging 目录，完成哈希和重载检查后再原子发布；失败时回滚，不留下半成品。

CLI 支持因子、指数、开始/结束日期、调仓频率、输出目录和是否生成报告。最终默认与导师验收口径一致。

## 5. 第一版为什么被判定为不可接受

第一轮建设完成后，历史记录显示单元/回归测试通过，CLI 能跑完真实窗口，并生成 32 个产物。初始构建阶段曾报告 38 项测试通过；独立审计在其实际收集范围内复现为 34/34 通过。无论具体收集数为何，核心问题相同：测试只证明已覆盖路径通过，没有覆盖关键对抗性财务场景。

独立审计按权威规范、当前源代码和真实数据运行约 7,056 行实现/测试/脚本，并运行额外数值探针，最终判定 20 条完成条件仅 4 条通过。关键问题如下：

1. 原始股数与复权股数换算方向反了，10,000 账户无市场波动也跌到 3,250。
2. 滑点通过成交价计入后又从现金扣除，正确 9,910 被算成 9,820。
3. 非交易日月度权重快照没有触发基准重置，正确 5.5% 被算成 1.0%。
4. 超额净值错误复利算术日超额，1.047619 被算成 1.05。
5. 结果哈希只覆盖少数输出，未覆盖订单、成交、持仓、现金、成本和诊断。
6. `run_id` 与 `artifact_uri` 使用不同生成 ID；32 个文件只有 15 个被 manifest 登记。
7. 默认日期、OOS、频率和成交价与最终确认口径冲突。
8. 现金缓冲被应用两次。
9. `drop_incomplete` 计算了所需 lag，却没有真正移除末端信号。
10. 周/月频选股诊断偷偷按日更新目标。
11. 换手把被动价格漂移算成交易。
12. 配置接受 `index_weight`，引擎却拒绝；最大换手只报告不执行。
13. 持仓缺数量、成本基础、未实现 PnL 和权重。
14. 排除原因诊断始终为空。
15. 缺少导师批准的 BP 因子。
16. 只保存主动风格，没有组合和指数风格序列。
17. 图表缺少完整风格、换手和 ADV 信息，中文字体损坏。
18. Markdown 缺指标、时间线、权重方法、流动性和完整未成交说明。

因此，第一版真实数据结果虽然能运行，但财务口径无效。审计时的日频 `-79.32%` 和月频 `-16.71%` 结果均被明确作废，不能继续引用。

## 6. 从审计失败到最终通过的逐批优化

### 6.1 第一批：修复所有下游指标依赖的会计和时间线

同时修复：

- 原始/复权股数换算。
- 显式现金费用与嵌入滑点分离。
- 现金缓冲只应用一次。
- 买卖段 PnL 用无滑点参考金额独立计算。
- 非交易日指数快照映射。
- 几何超额净值。
- 默认日期、OOS、日频和 VWAP。
- `drop_incomplete` 真正裁掉末端信号。

新增四个直接复现旧错误的对抗测试。完成后受影响的 26 项测试通过，四个数值探针全部从失败变为通过。

### 6.2 第二批：补齐可审计账户、约束和诊断

- 持仓增加原始/复权数量、成本、市值、未实现 PnL 和权重。
- 现金账本增加可用/冻结现金、已实现/未实现 PnL。
- 换手改为实际成交口径。
- 实现 `index_weight`。
- 实现目标换手缩放和实际成交换手预算。
- 完整保存选股排除原因。
- 新增 BP 因子。
- 选股诊断改为真实调仓日数量模拟。
- 指数退出在下一调仓日按可交易性强制处理并打标签。

执行层第二道换手限制解决了“目标换手不超限，但成交价格和滑点使实际成交超限”的边界问题。

### 6.3 第三批：数据、因子、结果和产物血缘

- 无 Git 元数据时，代码版本改为确定性的源树内容哈希。
- dirty Git 版本也包含源树哈希。
- 因子 values 每次重新计算内容哈希；调用方声明不一致时直接失败。
- 数据哈希覆盖所有实际消费字段、日期/列轴、dtype、空值、MultiIndex 和纳秒时间精度。
- 使用类型标签和长度前缀编码，消除简单字符串拼接的碰撞歧义。
- 结果哈希覆盖完整业务契约，而非五个摘要字段。
- 每次运行只生成一个 run ID，并贯穿 manifest 与 artifact path。

### 6.4 第四批：事务式数据和产物发布

- 行情摄取从“先删旧仓库”改为 staging -> 全量验证 -> 原子交换 -> 失败回滚。
- 启动摄取时先检测并恢复中断交换留下的备份。
- 日历缓存与源分区内容哈希绑定。
- 产物路径限制在输出根目录，阻止 `../`、绝对路径和子目录逃逸。
- JSON 将 NaN/Inf 规范化为 `null`，并用 `allow_nan=False` 强制标准 JSON。
- 报告在 staging 中完成后再原子发布。
- manifest 最后写，登记所有数据、报告和图表。

三组不完整旧产物没有被删除，而是移入 `artifacts/_quarantine_invalid_preverification_20260807/`，附带 README 明确标记不可用。

### 6.5 第五批：完整重载与字节级报告不变性

新增 `load_backtest_artifacts()`，从 Parquet/JSON 重建完整冻结结果；新增 `verify_backtest_artifact_roundtrip()`，比较内存对象与落盘对象，并验证所有 SHA-256。

这一步连续发现并修复了细小但重要的类型保真问题：

- JSON split 表首部为空导致列被加载为 `object`。
- 全字符串年份列被错误转为数值。
- 布尔值因 Python 中 `bool` 是 `int` 子类而被转成整数。
- rolling Alpha/Beta 图因持久化索引/类型差异而产生不同哈希。

最终重载后生成的 Markdown、JSON 和 14 张图表与原结果字节/哈希一致。

### 6.6 第六批：报告指标和视觉质量

审计报告层时又发现两项业务数据错误：

- “未成交金额”实际保存的是未成交股数。
- 成本侵蚀使用组合毛收益，而非组合相对基准的毛超额。

修复后，未成交金额由未成交股数乘参考价格计算；成本侵蚀按毛超额分母计算。报告扩展为包含完整时间线、选股、权重、绩效、IS/OOS、回归、风格、诊断、流动性、约束、定义和血缘。

视觉 QA 又发现样本内超额回撤仍使用旧算术超额，以及预启动全现金日期被算作现金比例违规。两者均在最终验收前修复并补测试。

### 6.7 第七批：真实数据约束证据

第一轮修复后的日频真实运行发现 428 条收盘后单票权重超过 5%。这些不是目标权重构造错误，而是价格漂移或未成交导致的真实结果。优化没有隐藏它们，而是：

- 保存每条日期、股票、观测值、上限和超额。
- 明确区分目标上限、收盘后漂移和未成交。
- 区分 ADV cap binding 与真实 ADV violation。

之后因重跑边界变化，最终日频报告为 430 条、月频为 108 条。Pandas 3 的 `stack()` 行为一度把月频 108 条膨胀为 1,861,242 条，因为 masked NaN 被保留。最终显式 `.dropna()`，并用“报告行数等于真实 mask 数”回归测试锁定。

### 6.8 第八批：Bugbot 和 Security Review 最终关闭

Bugbot 最终指出并关闭：

- 缺失 ADV 时不受限制成交：改为 `adv_missing` 拒绝。
- 调用方提供虚假因子哈希：重新计算并比对。
- 报告请求配置被忽略：PNG/SVG、Markdown/JSON 开关和表格限制均真实生效。
- 执行控制被记录但未实现：支持项强制执行，不支持项构造时失败。
- 滑点后成交额可能超过 ADV：cap 按滑点后的实际成交名义金额执行。
- 报告错误写成开盘价：根据 `fill_price_field` 动态披露。
- 缺手工/golden 证据：增加五日硬编码账本和真实首日现金/NAV 独立复算。

Security Review 最终指出并关闭：

- DataFrame 哈希结构歧义。
- 指数 ID 和权重输入缺乏约束。
- 日历缓存只依赖弱元数据。
- 非标准 JSON。
- `artifact_uri` 与发布目录不一致。
- dirty Git 版本不绑定源内容。
- 报告发布非原子。
- 行情摄取中断恢复不足。
- 报告尺寸、DPI、格式、行数和必要章节缺校验。
- 旧无效产物可能被误用。

## 7. 测试体系如何从“绿色但不正确”演进为可信证据

### 7.1 单元测试

最终单元测试覆盖：

- 时钟禁止同日成交。
- 所有配置范围和 NaN/Inf 拒绝。
- 默认口径与导师验收一致。
- 选股 30%、方向、可交易过滤和并列确定性。
- 因子强度、等权兜底和指数权重。
- 手续费、印花税时变和买卖方向。
- 整手、零股、现金、换手和账户恒等式。
- 复权股数无 NAV 跳变。
- 滑点不重复扣除。
- ADV 缺失拒绝和滑点后 ADV 上限。
- 指数退出。
- 几何超额和超额回撤。
- OLS/Newey-West、风格恒等式。
- 所有演示因子的未来截断不变性。
- 非交易日基准重置。
- 末端信号裁剪。
- 日历缓存篡改。
- 路径穿越、严格 JSON、哈希碰撞和摄取回滚。

### 7.2 回归测试

回归层使用少量股票、固定价格、固定订单和手算结果验证：

- 每日账户恒等式。
- 首日 PnL。
- 完整五日 golden 现金账本和持仓。
- 结果哈希稳定性。
- 约束明细行数与真实布尔 mask 一致。

“零成本”fixture 曾只关闭佣金和滑点，却遗漏过户费和印花税。测试失败后把所有费率档位一并置零，使它真正成为独立零成本基准。

### 7.3 集成和真实数据测试

- 真实数据端到端回测。
- 有成本收益不得优于相同条件的零成本收益。
- IS/OOS 切片。
- 五个演示因子全部跑通。
- 0.1% 与 100% ADV 上限真实改变成交和 cap 事件。
- 报告器执行前后结果哈希不变。
- 落盘后完整重载，重新生成 Markdown/JSON/14 图，哈希完全一致。
- 报告请求测试：只生成 14 个 SVG，不写 Markdown 和摘要 JSON。

### 7.4 最终测试证据

- 最终自动测试：81/81 通过。
- 最终修改后再次完整运行：81/81 通过。
- Python `compileall`：通过。
- `ruff` 和 `mypy`：项目环境未安装，因此没有虚报成功。
- 最终审计扫描：引擎无 reporting/Matplotlib 依赖。
- 18 个 JSON 文件：严格解析通过。
- 28 张 PNG：像素方差非零并完成人工视觉检查。
- 两个运行各有 37 个文件，其中 manifest 登记 36 个引用；共 72 个引用哈希通过。

2026-08-07 本文编写时再次执行轻量现状检查：81 项测试可成功收集，`compileall` 通过，两个验收目录的 72 个文件内容哈希仍全部匹配。

## 8. 最终真实数据验收结果

### 8.1 日频 20 日反转

- 产物目录：`artifacts/acceptance_daily_reversal_20260807`
- Run ID：`lof_20260807T042719Z_a9d0f46d`
- Result hash：`d76f383c625e647683b0293f777a8bdfba44883a37d22ef04c56aaf176ec2bf6`
- Code version：`source-38d893060ffee9b3`
- Factor hash：`e5b13db7e382473a8793648c0ee725a7aba18472ae8f025529b2b4502484108a`
- 有效日期：`2019-01-02` 至 `2026-03-31`，1,755 日。
- 组合累计收益：-35.1964%。
- 基准累计收益：+111.3119%。
- 几何超额：-69.3327%。
- 年化 Alpha：-15.2579%。
- Beta：1.019717。
- 最大账户残差：`6.21e-12` bps。
- 正成交记录：238,497 / 288,621。
- 最大实际 ADV 参与率：10%。
- ADV 真实违规：0。
- ADV cap binding：2。
- 缺失 ADV 拒绝：13 笔，0 笔绕过限制成交。
- 收盘后单票权重漂移/未成交超限：430 条，完整保留。

该策略毛收益为正但日频换手和成本非常高，最终净表现显著为负。这说明框架成功暴露了不可实现性，而不是用目标权重收益掩盖执行摩擦。

### 8.2 月频 BP

- 产物目录：`artifacts/acceptance_monthly_bp_20260807`
- Run ID：`lof_20260807T043014Z_58e4641f`
- Result hash：`ff6af6de7cd822a7238a691856b5cd4b454ee237fcf6d72ba3b30d4dd915aa32`
- Code version：`source-38d893060ffee9b3`
- Factor hash：`8f11ffba02049cc30e0fc5d93c98bab785c31c339facfb77fa09ff77cea3d6db`
- 有效日期：`2019-01-02` 至 `2026-03-31`，1,755 日。
- 组合累计收益：+103.5136%。
- 基准累计收益：+111.3119%。
- 几何超额：-3.6904%。
- 年化 Alpha：+2.3827%。
- Beta：0.759997。
- 最大账户残差：`5.64e-12` bps。
- 正成交记录：12,364 / 13,344。
- 最大实际 ADV 参与率：10%。
- ADV 真实违规：0。
- ADV cap binding：4。
- 非调仓日最大换手：0。
- 收盘后单票权重漂移/未成交超限：108 条。

这组结果证明月频时钟没有暗中日频调仓，BP 输入被正确接入，且同一数据/执行/报告链路可处理明显不同的换手模式。

## 9. 独立现金和 NAV 复算

复算没有使用现金账本中的“期望值”，而是直接从成交和成交后持仓重新计算：

```text
expected_cash
= previous_cash
 + sell_notional
 - buy_notional
 - explicit_commission
 - stamp_duty
 - transfer_fee

expected_NAV
= expected_cash + sum(post_trade_holding_market_value)
```

日频反转首个实际成交日 `2019-01-03`：

- 前一日现金：100,000,000.000000。
- 买入名义金额：96,856,884.570440。
- 卖出名义金额：0。
- 显式费用：26,151.358834。
- 独立期望现金：3,116,964.070726091。
- 账本现金：3,116,964.070726068。
- 持仓市值：96,082,861.804127。
- 独立期望 NAV：99,199,825.874853。
- 账本 NAV：99,199,825.874853。

月频 BP 首个实际成交日 `2019-02-01`：

- 前一日现金：100,000,000.000000。
- 买入名义金额：98,973,255.813561。
- 卖出名义金额：0。
- 显式费用：26,722.779070。
- 独立期望现金：1,000,021.407369734。
- 账本现金：1,000,021.407369733。
- 持仓市值：99,956,552.340533。
- 独立期望 NAV：100,956,573.747903。
- 账本 NAV：100,956,573.747903。

差异小于一百万分之一货币单位，只来自浮点舍入。

## 10. 最终产物契约

每个验收运行包含 37 个文件，其中 `run_manifest.json` 登记另外 36 个引用：

```text
run_manifest.json
request_config.json
selected_members.parquet
target_weights.parquet
actual_weights.parquet
orders.parquet
fills.parquet
holdings.parquet
cash_ledger.parquet
daily_returns.parquet
costs.parquet
accounting_identity.parquet
style_exposures.parquet
exclusion_reasons.parquet
unfilled_summary.parquet
alpha_beta.json
style_exposure_report.json
index_selection_report.json
performance_report.json
constraint_reports.json
diagnostics.json
backtest_report.json
backtest_report.md
charts/14 个图表
```

Manifest 记录：

- `run_id`、`strategy_id`、factor/index ID。
- 完整解析配置和业务摘要。
- 代码版本和配置版本。
- 每个数据源的 URI、行列数、日期范围、内容哈希和说明。
- 因子数据版本、代码版本和实际内容哈希。
- 结果哈希。
- 每个产物的 URI、SHA-256 和字节数。
- 数据限制与执行口径披露。

## 11. 导师 20 条完成条件的最终对应关系

1. 最小请求只需最终因子和指数 ID，其余有完整默认配置。
2. 引擎不组合、学习或反转因子。
3. PIT 成分、可交易和有效因子交集选前 30%，并列确定。
4. 因子强度权重、cutoff、兜底、目标权重和排除原因均持久化。
5. T 收盘信号、T+1 成交，所有因子通过未来截断测试，末端不完整信号被删除。
6. 目标、订单、成交、持仓、成本基础、市值、现金和账本均可重载。
7. 停牌、涨跌停、指数退出、ADV、取消和拒绝原因均有实现和测试。
8. 收益来自实际持仓；显式费用与嵌入滑点不重复。
9. 毛/净/基准/超额净值和三类回撤均持久化和绘图。
10. 全样本、IS、OOS 绩效和定义完整。
11. OLS/NW、R²、滚动估计和贡献完整。
12. 组合、指数和主动风格完整，缺失风格显式标注。
13. 选股、覆盖、持仓、换手、成本、ADV 和未成交诊断完整。
14. Markdown 包含关键指标，Parquet/JSON 保留日级和事件级原始输出。
15. 数据、因子、配置、代码、结果和产物哈希完整。
16. DataPortal 统一加载并登记全部输入。
17. 引擎无报告或绘图库依赖，返回冻结完整结果。
18. 报告器只消费结果；报告前后结果哈希一致；重载报告一致。
19. 报告字段可追溯至结果组件，持久化组件通过 round-trip 和 inventory。
20. 单元、集成、回归、对抗、真实比较、视觉、哈希和人工复算全部通过。

## 12. 当前已知限制

### 12.1 数据限制

- 行情库缺历史退市股票，存在幸存者偏差。
- 历史 ST 状态不可靠，无法完全精确重建 5% 涨跌停规则。
- 46 只历史指数成分缺行情，最大单期权重缺口 5.73%。
- 中证 500 月度 PIT 数据缺 12 个月；当前只向未来沿用最近已知快照。
- 官方全收益指数点位缺失，当前为 PIT 权重合成基准，存在跟踪误差。
- 缺显式公司行动事件；系统在复权价格/复权股数空间保持价值一致。
- 缺 PIT 行业分类，行业偏离约束关闭。
- 风格为简化代理，非 Barra；Growth、Quality、Leverage 缺失。
- PB 是否完全 PIT 依赖数据商历史重述政策。

### 12.2 研究解释限制

最终验收证明框架、会计、执行、分析、报告和血缘在现有数据范围内正确。它不证明反转或 BP 策略具有可交易 alpha，也不证明结果无幸存者偏差。尤其日频反转的成本结果表明，一个毛表现尚可的信号可能被实际执行摩擦完全吞噬。

## 13. 使用方法

进入当前项目目录：

```bash
cd /Users/a1/Desktop/添橙/TC5days20260806/quant_backtest_system
source .venv/bin/activate
```

运行完整默认日频反转：

```bash
python -m qbt.cli.main --factor reversal_20d
```

运行月频 BP：

```bash
python -m qbt.cli.main \
  --factor bp_1_over_pb \
  --start 2018-01-01 \
  --end 2026-03-31 \
  --freq monthly
```

运行测试：

```bash
.venv/bin/python -m pytest tests -q
```

重新摄取数据应使用 `scripts/ingest_data.py` 或 `qbt.data.ingest_prices.ingest_prices()`，不要直接覆盖 `warehouse/daily_prices`。

## 14. 后续扩展的正确顺序

1. 接入官方全收益指数点位，并保留当前合成基准作为对照。
2. 补历史退市股票、证券主档和 PIT ST 状态，再重跑所有验收。
3. 接入显式分红、送转、配股等公司行动事件，验证与复权空间结果的一致性。
4. 接入 PIT 行业和机构风格数据，再开启行业/主动风格硬约束。
5. 增加执行模型时，先扩展配置契约和失败模式，再实现引擎，最后补对抗性账本测试。
6. 增加报告字段时，先把字段冻结进 `LongOnlyFactorBacktestResult`，报告器不得自行重新计算。
7. 支持项目移动时，设计 manifest URI 的可验证重定位协议，同时保留发布时原始 URI。

## 15. 关键工程经验

1. “测试全绿”不等于财务正确。必须加入能手算的极端情形。
2. 目标权重不是持仓，理论收益不是可实现收益。
3. 复权价格必须与股数空间严格配对，否则会凭空创造或消灭资产。
4. 滑点要么进入成交价，要么作为现金费用，不能两边都算。
5. 算术日超额和几何超额净值是不同对象，报告必须同时定义清楚。
6. 缺失流动性不是无限流动性；保守系统应拒绝并披露。
7. 真实约束结果不能被“优化掉”。目标限制、执行限制和收盘后漂移应分别报告。
8. 哈希只有覆盖完整业务契约和结构信息才有审计意义。
9. 原子发布、严格 JSON、完整 manifest 和可重载结果共同构成可复现性。
10. 已知数据不足时，明确缺失比伪造代理更可靠。

## 16. 最终状态摘要

该项目经历了两个本质不同的“完成”：

第一版完成了架构和功能表面，能够运行和出报告，但对抗性审计证明核心会计、基准和血缘仍不可靠，因此所有早期绩效数字被作废。

最终版在此基础上重新建立了可验证的会计、执行、约束、分析、持久化和报告契约。81 项测试、两个 1,755 日真实运行、72 个产物哈希、28 张图、完整重载、独立现金/NAV 复算和 20 条导师条件共同支撑最终验收。

最重要的成果不是某个因子的收益，而是系统终于能诚实地区分：信号有多强、目标想买什么、市场实际成交了什么、成本侵蚀了多少、哪些约束生效或失败、结果能否被完整重建，以及哪些结论因为数据缺口仍然不能下。


---

# 2026-08-09 更新：全 A 默认基准与第二轮质量收敛

本节作为当前版本的最新技术记录，覆盖默认指数能力、交易所数量规则、第二轮审计修复、静态质量门槛和三组最终验收产物。前文旧的 81 测试、两个运行和 72 个引用是历史阶段数据；本节的 100 测试、三组运行和 108 个引用是当前验收基线。

## 17. 默认全 A 等权基准

研究请求的 `index_id` 现在可省略。省略时由 `LongOnlyFactorBacktestRequest` 规范化为 `ALL_A_EQ`，展示名称为“全 A 等权”；空字符串和非字符串仍然失败。原有明确指数 ID 的 PIT 月度成分路径保持不变。

CLI 同步支持两种等价写法：

```bash
python -m qbt.cli.main --factor reversal_20d
python -m qbt.cli.main --factor reversal_20d --index ALL_A_EQ
```

CLI 的 choices 同时包含 `ALL_A_EQ` 和原有指数；帮助文本明确说明省略时的默认值。为了避免 `python -m qbt.cli.main` 触发模块预加载警告，`qbt.cli` 改成懒加载 `main` 包装器。

### 17.1 股票池发现与 provenance

全 A 模式扫描 `warehouse/daily_prices/*.parquet` 的 `asset_id` 列，与传入因子的资产列合并去重。没有行情分区、分区无法读取或最终没有资产时均带明确原因失败，不再静默跳过坏分区。

membership 使用行情面板的 `listed_first`/ `listed_last` 构造，只有上市区间内的资产进入当日分母。全 A 的 membership、weight、benchmark returns 都分别写入 DatasetRef，benchmark 来源固定为 `synthetic:all_a_equal_weight_daily`；CSI 500 合成基准固定为 `synthetic:pit_monthly_weights`，不再错误复用来源标识。

### 17.2 每日等权和停牌复牌

```text
observed_by_date = adj_close.notna().cummax()
valid = listed_member AND observed_by_date
filled_close = adj_close.ffill()
daily_return = filled_close.pct_change().fillna(0)
weight = valid / valid.sum(axis=1)
benchmark_return = sum(weight * daily_return)
```

已上市但停牌的股票留在等权分母，停牌日收益为 0，复牌日从最后有效收盘价计算反弹。未来尚未出现首个报价的股票不提前进入。合成探针结果为 `[0%, 5%, 5%]`；未来上市股票的独立测试也通过。

## 18. 交易所数量规则和执行层再审计

第二轮独立探针发现，之前的验收产物仍包含普通板非整手买入和小数科创板买入：旧日频有 95,328 笔普通非整手买单，旧月频有 6,686 笔，且存在小数 STAR 买入。因此旧 acceptance 目录被拒绝并全部重建。

执行层现在区分最小数量和递增步长：

- 普通 A 股：最小 100，递增步长 100。
- 科创板 688/689：最小 200，达到 200 后按 1 股递增。
- 卖出保留零股清仓能力。

统一 `_round_buy_quantity` 应用于初始订单、增量订单、ADV cap、现金 cap 和换手 cap。订单差额而非仅目标股数必须合法：

- 科创板 118 股增量被拒绝，原因 `below_min_buy_quantity`。
- 普通股票 189 股增量成交 100，剩余 89 记录为 `lot_size_rounding`。
- 真实运行共检查 261,177 笔正成交，小数成交、非法普通买入和非法 STAR 买入均为 0。

## 19. IPO 窗口、排除链和会计精度

### 19.1 IPO 涨跌停窗口

旧的面板位置映射会把回测窗口之前上市的股票错误地当成窗口首日新股。最终只在上市日真实落入当前面板窗口时应用 IPO 无涨跌幅限制：主板/北交所首个交易日，创业板/科创板上市后前 5 个实际面板交易日。窗口之前已上市的股票不再获得重复例外。

### 19.2 资产级排除原因

选股结果新增确定性的长表：

```text
date | asset_id | reason
```

原因包括 `not_tradable_and_invalid_factor`、`not_tradable`、`invalid_factor` 和 `below_selection_cutoff`。长表使用 categorical 列降低真实数据内存占用，并同时写入 diagnostics、Parquet、Markdown 汇总和重载结果。

### 19.3 全平仓比例噪声

真实数据曾出现 `0.00153 bps` 的会计残差。原因是带微小噪声的 raw/adjusted ratio 在全平仓时把重建原始股数换算成略大的 adjusted 数量，而持仓更新又将余额 clamp 为 0。修复后现金成交继续使用真实整数原始股数，内部 PnL 对账使用精确剩余 adjusted balance。新增三日回归后，残差低于 `1e-6 bps`。

## 20. 工程质量门槛

第二轮质量收敛完成了以下门槛：

- 100/100 测试通过，耗时 318.48 秒。
- Branch-aware coverage：83%，覆盖 36 个源文件。
- Ruff 0.14.10：src、tests、scripts 全部 clean。
- mypy 1.19.1：Python 3.14 下 36 个源文件通过。
- `compileall`、`pip check`、`git diff --check` 通过。
- `python -W error -m qbt.cli.main --help` 通过。
- Engine AST 扫描无 reporting 或 Matplotlib 依赖。
- 27 个 JSON 严格解析通过。
- 42 张 PNG 非空并完成代表性视觉检查。
- 三组结果均可安全重载。

mypy 暴露并修复了 `ExecutionOutput` 动态字段、`_build_price_frame` 错误返回注解、optional index 和 disclosure mapping 类型收窄问题。行情摄取模块也删除了 import 时全局 suppress `FutureWarning` 的副作用。

工作区改名后，`.venv/bin` 的旧 shebang 也被重新生成；现在 `pytest`、`ruff`、`mypy` 和 `qbt-backtest` 都指向当前项目路径。

## 21. 三组最新真实数据运行

### 21.1 日频 CSI 500 20 日反转

- Run ID：`lof_20260807T143404Z_26d2e10b`
- Result hash：`b936afba7cf1fe42df3ddfb8257610d173d15d35e22c4ab9d0e0e422368dbdd3`
- 有效区间：2019-01-02 至 2026-03-31，1,755 日。
- 组合 / 基准 / 几何超额：-35.7557% / +111.3119% / -69.5974%。
- 年化 Alpha / Beta / R²：-15.3600% / 1.019111 / 0.806686。
- 正成交 244,503；普通买入 102,444；STAR 买入 5,420。
- 小数或非法买入：0；低于最小数量拒绝 386；整手舍入未成交 1,697。
- 最大 ADV 参与率 9.9999979%，违规 0；最大残差 `4.87e-12 bps`。

### 21.2 月频 CSI 500 BP

- Run ID：`lof_20260807T144100Z_5b9c9f8c`
- Result hash：`f35b0acea793f4f22285d7125145be7665cd798b6b2cdb20477610799cbbe974`
- 有效区间：2019-01-02 至 2026-03-31，1,755 日。
- 组合 / 基准 / 几何超额：+103.4827% / +111.3119% / -3.7050%。
- 年化 Alpha / Beta / R²：+2.3822% / 0.759792 / 0.645764。
- 正成交 12,564；普通买入 6,962；STAR 买入 34。
- 小数或非法买入：0；低于最小数量拒绝 15；整手舍入未成交 4,043。
- 最大 ADV 参与率 9.9999961%，违规 0；最大残差 `6.27e-12 bps`。

### 21.3 省略指数的全 A 等权

- Run ID：`lof_20260807T144809Z_b7a6a7a6`
- Result hash：`23d34750955d9cbb772012c8076e220c2250b63b58b07dfba1e11a6a4c82e94a`
- 解析指数：`ALL_A_EQ`。
- 有效区间：2025-01-02 至 2025-03-31，57 日。
- benchmark source：`synthetic:all_a_equal_weight_daily`。
- 组合 / 基准 / 几何超额：+12.4486% / +7.5794% / +4.5262%。
- 年化 Alpha / Beta / R²：+41.7813% / 0.532103 / 0.462007。
- 正成交 4,110，全部满足交易所规则；最大 ADV 参与率 2.4584%；最大残差 `1.19e-11 bps`。

## 22. 独立复算和产物一致性

三组运行均由 fills 和 post-trade holdings 独立重算现金与 NAV，最大差异不超过 `1.31e-7` 货币单位。OLS Alpha、Beta、R²、总收益、年化收益、年化波动和最大回撤从 `daily_returns.parquet` 独立重算后，全部字段差异低于 `2e-12`。

三组目录共 111 个文件，每个目录 37 个文件，其中 manifest 登记 36 个引用，共 108 个引用。当前路径下每个引用的文件存在、字节数和 SHA-256 均匹配；27 个 JSON 标准解析通过，42 张图像非空，三个结果均可由 `load_backtest_artifacts()` 重建。

## 23. 当前研究边界

全 A 等权是基于可观察上市行情的合成基准，不是官方指数。官方全收益指数、历史退市和 ST、显式公司行动、PIT 行业分类和机构级 PIT 风格数据仍缺失。因此三组运行证明的是实现、账户、执行、报告、血缘和可重现性，不是无偏生产策略收益。

## 24. 2026-08-13 更新：每期持仓分析

### 24.1 更新目的

为增强回测结果的可审计性，新增**每期持仓分析**，对每一个调仓成交日、每一只相关股票，记录：

- 调仓前实际持仓（原始股数 + 复权股数）
- 目标持仓（原始股数 + 复权股数）
- 下单数量
- 实际成交数量
- 调仓后实际持仓
- 动作方向、成交状态、未成交原因

这样可以直接回答类似问题："8.10 中际旭创 持仓 100 股、目标 220 股、实际买入 100 股" 或 "卖出时持仓 100 股、目标 0 股、实际卖出 100 股"。

### 24.2 实现要点

1. **在执行引擎内部生成**，而非事后从 orders/fills/holdings 反推。
   - `src/qbt/engine/execution.py` 的 `_rebalance_day` 中已经精确掌握 `raw_held`（调仓前原始股数）、`raw_target_shares`（目标原始股数）、`delta_raw`/`gap_raw`（卖出/买入数量）、成交后 `shares`。
   - 新增 `PositionPeriodRecord` 记录每个调仓日单只标的的完整迁移。
   - 通过 signed quantity 保证 `pre + fill = post` 严格成立。

2. **新增契约**
   - `src/qbt/contracts/records.py`：`PositionPeriodRecord` dataclass 与 `position_period_to_frame()`。
   - `src/qbt/contracts/reports.py`：`LongOnlyFactorBacktestResult.position_period_analysis: pd.DataFrame`。
   - `src/qbt/engine/backtester.py`：将 `execution.position_period_records` 转换为 DataFrame 后写入结果。

3. **动作分类规则**
   - `buy`：目标 > 当前 + 容差
   - `sell`：目标 < 当前 - 容差，且未完全清仓
   - `exit`：目标 ≈ 0 且原持仓 > 0（含指数强制退出）
   - `hold`：目标 ≈ 当前，无订单

4. **状态字段**
   - `filled`：完全成交
   - `partial`：部分成交
   - `rejected`：全部拒绝/未成交
   - `no_order`：持仓与目标一致，无需下单

### 24.3 新增产物

每个运行现在产出 39 个文件（原 37 个），新增：

- `position_period_analysis.parquet`：逐日逐标的持仓迁移明细
- `position_analysis_summary.json`：动作分布、状态分布、买卖总股数、平均成交比例

`run_manifest.json` 自动登记这两个新产物及其 SHA-256。

### 24.4 报告展示

`src/qbt/reporting/md_builder.py` 新增 `## 8. 调仓持仓分析` 章节，包含：

- 总记录数、调仓日数、涉及标的数
- 买入 / 卖出 / 清仓 / 持仓不变 的计数
- 完全成交 / 部分成交 / 拒绝 的计数
- 买入总股数、卖出总股数
- 按成交日排序的明细表：

| 成交日 | 标的 | 动作 | 调仓前(股) | 目标(股) | 下单(股) | 成交(股) | 调仓后(股) | 成交价 | 成交比例 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2025-01-03 00:00:00 | 000002.SZ | buy | 0 | 5,500 | 5,500 | 5,500 | 5,500 | 7.0771 | 1.0000 | filled |

### 24.5 测试与验证

- 新增单元测试：
  - `tests/unit/test_execution_costs.py::test_position_period_records_capture_pre_target_fill_post`
  - `tests/unit/test_execution_costs.py::test_position_period_records_full_liquidation_is_exit`
- 更新集成测试 `tests/integration/test_end_to_end.py::test_reporting_does_not_mutate_result_and_reloaded_result_is_identical`：
  - 断言 `position_period_analysis.parquet` 与 `position_analysis_summary.json` 存在
  - 断言 Markdown 包含 "调仓持仓分析"
  - 断言重载后的 `position_period_analysis` 与原结果逐行相等
- 全量测试：100/100 通过（单元 70 + 回归 22 + 集成 8）。
- CLI 真实数据验证：`python -m qbt.cli.main --factor reversal_20d --start 2025-01-01 --end 2025-03-31 --index ALL_A_EQ --freq daily`，产物 39 个，报告正常。

### 24.6 已知影响

- `result_hash` 因新增 `position_period_analysis` 字段而变化；旧 acceptance 产物的哈希不再匹配，这是预期行为。
- 产物契约从 37 个文件变为 39 个文件；`verify_backtest_artifact_roundtrip` 已同步校验新增文件与清单一致性。
- 引擎仍然不依赖 `qbt.reporting` 或 Matplotlib。

### 24.7 关键文件变更

- `src/qbt/contracts/records.py`
- `src/qbt/contracts/__init__.py`
- `src/qbt/contracts/reports.py`
- `src/qbt/engine/execution.py`
- `src/qbt/engine/backtester.py`
- `src/qbt/reporting/artifacts.py`
- `src/qbt/reporting/md_builder.py`
- `tests/unit/test_execution_costs.py`
- `tests/integration/test_end_to_end.py`

### 24.8 精简交付副本同步

同步在 `quant_backtest_system_clean` 中完成：

- 将上述 7 个源码文件（不含测试，因为精简副本移除了测试）同步复制到
  `quant_backtest_system_clean/src/qbt/` 对应位置。
- 保留 `quant_backtest_system_clean` 原有的 CLI 默认指数目录
  (`data/index_membership_source`) 等交付特定差异，未做改动。
- 重新生成 `SHA256SUMS`。
- 更新 `DELIVERY_MANIFEST.md` 的交付验证日期与说明，产物数量从 37 变为 39。
- 在 `quant_backtest_system_clean` 中执行显式中证 500 CLI 回测验证：
  57 个交易日，39 个产物，账户恒等式残差 `0.000000 bps`。

## 25. 2026-08-18 更新：外部因子值直接回测、Web 界面指标修复、费用可调与历史持仓页

### 25.1 更新目的

本轮更新解决四项需求，全部按"每新增一小部分就带入真实数据测试，无误后再继续"的增量纪律推进：

1. **外部因子值直接回测**：支持把已经算好的因子值（parquet 宽表，非公式）直接喂给回测器，并与单因子（公式因子）回测统一到同一入口与同一套界面（参考外部导出包 `factor_values_export` 的处理方式）。
2. **Web 指标显示修复 + 交易费用可调**：概览页"绩效指标详情"表存在两类格式错误 —— "总成本"行样本内/样本外列显示成 `1896365896.36%`（金额被当百分比）、"Sharpe"行显示成 `39.75%`（比率被当百分比）；同时把交易费用（滑点、佣金、印花税、过户费）全部改为界面可调。
3. **净值图改收益率口径**：概览页"策略 vs 基准/超额净值"图的 nav 序列从净值（1.0 起）改为累计收益率（0.0 起），纵轴百分比刻度。
4. **历史持仓页**：新增独立页签，展示最新两期调仓记录（标的、方向、成交股数、成交价、成交金额、佣金、印花税、过户费、费用合计），并提供"下载全部历史 (CSV)"按钮。

涉及三处代码库同步更新：`quant_backtest_system`（主系统）、`quant_backtest_system_clean`（精简交付副本，仅 qbt 核心部分）、`quant_backtest_web`（FastAPI + React 可视化系统）。

### 25.2 任务一：外部因子值直接回测（qbt 核心）

#### 25.2.1 格式契约

参考外部导出包 `factor_values_export (2)` 的实际结构，约定导入格式为：

| 项目 | 约定 |
| --- | --- |
| 文件 | Parquet 宽表，单文件单因子 |
| 行 | 交易日 `DatetimeIndex`（外部导出常见 `datetime64[us]`，字符串日期索引也可，能转即可） |
| 列 | `asset_id` 字符串（如 `000001.SZ`），非空、不允许重复 |
| 值 | 数值型因子分值，缺失保留 NaN（选股层由 `missing_policy` 剔除），Inf 统一转 NaN |
| 方向 | 回测前显式声明 `direction=±1`，系统不做任何隐式方向推断 |
| 配套 | 导出包附 `因子清单.csv`（因子ID/因子名称/所属用户/来源回测），Web 导入时由用户在表单补齐名称与来源 |

#### 25.2.2 新模块 `src/qbt/factors/values.py`

三个公开 API：

- **`FactorValuesStats`**（frozen dataclass）：`n_dates / n_assets / date_start / date_end / coverage_ratio / n_duplicate_columns`，供上传体检与界面展示。
- **`inspect_factor_values(source)`**：读入并体检，不构建 FactorFrame。上传校验用它快速失败。
- **`load_factor_values(source, *, factor_id, direction, description, data_version, code_version, metadata) -> FactorFrame`**：构建与演示因子完全同构的 `FactorFrame`（`missing_policy="exclude_from_eligible"`、`content_hash=hash_frame(values)` 每次重算、metadata 记录来源路径/形状/区间/覆盖率与 `direction_locked_before_backtest: True`）。

内部规范化函数 `_coerce_wide_matrix` 的校验规则全部**显式失败、不静默修复**：

| 校验 | 失败行为 |
| --- | --- |
| 必须是二维 DataFrame 且非空 | TypeError / ValueError |
| 行索引可转日期 | TypeError（字符串索引逐元素解析） |
| 日期无重复 | ValueError（列出前 3 个重复日期） |
| 列名非空字符串 | ValueError |
| 列名无重复 | ValueError（重复会让选股并列判定不确定，列出前 5 个） |
| 值可转数值 | TypeError（object 列在值本身可转数值时放行，兼容部分导出工具） |
| 行列排序确定化 | 日期升序、列升序，保证内容哈希稳定 |
| Inf 处理 | `values.where(np.isfinite(values))` 统一转 float64 原生 NaN |

实现中修掉的两个坑：

- **pd.NA 替换崩溃**：最初用 `values.replace([inf, -inf], pd.NA).astype("float64")`，pd.NA 使列变 object 再转 float64 直接抛错。改为 `values.where(np.isfinite(values))`，float64 原生 NaN，一步到位。
- **UserWarning 破坏 `-W error` 门槛**：字符串索引 `pd.to_datetime` 逐元素解析会发 UserWarning，在严格模式下变成异常。用 `warnings.catch_warnings() + simplefilter("ignore", UserWarning)` 包裹，转换失败本身仍按 TypeError 处理。
- **mypy 惯用法报错**：`seen.add(c)` 作为表达式触发 func-returns-value 检查，改显式循环收集 `dup_assets`（顺带解决与索引重复检测里 `dup` 变量重名）。

#### 25.2.3 CLI 接入

`src/qbt/cli/main.py`：

- `--factor` 改为可选，新增 `--factor-values`（路径）、`--factor-id`（缺省取文件名 stem）、`--factor-direction`（±1）、`--factor-desc`。
- 互斥校验：`if bool(args.factor) == bool(args.factor_values): p.error("--factor 与 --factor-values 必须且只能提供一个")`。
- 因子值分支**不加载行情面板、不推导资产清单**（这些是公式因子的计算输入）；FactorFrame 直接由 parquet 构建，`data_version="cli"`。回测本身仍由引擎正常执行，选股时以因子覆盖资产与指数成分求交。

#### 25.2.4 测试

新增 `tests/unit/test_factor_values.py` 共 13 个测试：构建与元数据、内容哈希稳定（同数据两载哈希相等）、direction 校验（0/None/2 拒绝）、文件不存在、非日期索引、重复日期、重复列、非数值列、空 factor_id、Inf→NaN、stats 字段、内存 DataFrame 与磁盘 parquet 一致、oriented 方向翻转语义。

全量测试 **115/115 通过**（单元 82 + 回归 22 + 集成 8 + 新增 13 ——含新增后总数），pytest/ruff/mypy 三道质量门槛全绿（`execution.py` 存量 20 个 mypy 告警是 python-version 3.12 下才暴露的历史问题，项目官方配置 3.11，与本次改动无关，未触碰）。

#### 25.2.5 CLI 真实数据验证

用参考导出包里的真实因子值（`u5_factor`，名称 gj_alpha80 反向）跑通：

```bash
python -m qbt.cli.main --factor-values <parquet> --factor-direction 1 \
  --start 2024-01-02 --end 2024-04-09 --index 000905.SH --freq monthly
```

结果：58 个交易日，组合 +11.06%，基准 -2.61%，超额 +14.04%，账户恒等式残差 `0.000000 bps`，39 个产物，报告正常。

#### 25.2.6 精简交付副本同步与一次覆盖事故

同步到 `quant_backtest_system_clean`：`values.py`、`factors/__init__.py`、CLI（**保留 clean 交付特定的默认指数目录 `data/index_membership_source`**，主系统版默认父目录 `指数月度成分股`），`SHA256SUMS` 重新生成 79 文件（78+1），`shasum -c` 全过；`DELIVERY_MANIFEST.md` 新增"增量更新（2026-08-18）：外部因子值直接回测"章节。

**事故记录**：同步 CLI 时盲目 `cp`，把 clean 副本的默认指数目录覆盖成了主系统版本，破坏了交付副本的自包含性。处理：从备份 zip `quant_backtest_system_clean202608131811.zip` 提取原版 CLI，在其基础上重新套用因子值改动，diff 验证仅剩预期差异，clean 内实测 57 个交易日、39 产物、残差 `0.000000 bps`。教训：同步多副本时先 diff 确认交付特定差异，再做最小改动，绝不整文件覆盖。

### 25.3 任务二：Web 指标显示修复与交易费用可调

#### 25.3.1 根因

`Dashboard.jsx` 的指标表原实现对"全样本"列用 `fmtNum`，但"样本内/样本外"列一律 `fmtPct`。于是：

- 总成本（金额，如 33631116.813 元）→ 显示 `1896365896.36%`（原值先乘 100 再加 %）；
- Sharpe（比率，如 0.3975）→ 显示 `39.75%`。

#### 25.3.2 修复方案：按指标定义类型格式化

建立指标类型表，三列统一走 `fmtMetric(key, value)`：

```js
const METRIC_TYPES = {
  total_return: 'pct', annual_return: 'pct', annual_volatility: 'pct',
  sharpe: 'num', sortino: 'num', calmar: 'num',
  max_drawdown: 'pct',
  benchmark_annual_return: 'pct', benchmark_total_return: 'pct',
  excess_annual_return_geometric: 'pct', excess_total_return_geometric: 'pct',
  information_ratio: 'num',
  win_rate_daily: 'pct', win_rate_monthly: 'pct', win_rate_yearly: 'pct',
  turnover_annual_oneway: 'pct',
  total_cost: 'money',
  cost_erosion_ratio: 'pct',
  avg_holdings: 'int',
  avg_top10_concentration: 'pct', avg_cash_ratio: 'pct',
}
```

`pct` 百分比 / `num` 比率三位小数 / `money` 千分位金额两位小数 / `int` 千分位整数。类型归属与 `qbt` 指标语义一一对应（Sharpe/Sortino/Calmar/信息比率是比率不是百分比；总成本是货币金额；平均持股数是计数）。

#### 25.3.3 交易费用可调

**后端**（`engine.py` 新增 `_build_cost_config`）：

```python
def _build_cost_config(params):
    kwargs = {"commission_rate": ..., "slippage_bps": ...}
    if params.stamp_duty_rate is not None:
        kwargs["stamp_duty_schedule"] = (CostRate(date(1900, 1, 1), float(rate)),)
    if params.transfer_fee_rate is not None:
        kwargs["transfer_fee_schedule"] = (CostRate(date(1900, 1, 1), float(rate)),)
    return qbt.CostConfig(**kwargs)
```

设计要点：用户填了费率就用**单一档位 schedule**（起效日 1900-01-01 覆盖全区间）；留空则完全不传，保留 qbt 官方时变档位（印花税 2023-08-28 前 0.1% 后 0.05%；过户费 2022-04-29 前 0.002% 后 0.001%）。复用 `CostConfig` 现有 tuple-of-CostRate 机制，不动核心。

`models.py` 的 `RunConfig` 新增 `stamp_duty_rate / transfer_fee_rate`（`float | None`，ge=0 且有上限，None=官方时变）。

**前端**（`ConfigForm.jsx`）费用四项：滑点 (bps)、佣金率 (%)（默认 0.025）、印花税率 (%)（留空=官方时变，hint 标注档位）、过户费率 (%)（留空=官方时变，hint 标注档位）。百分比输入在提交时 `Number(v) / 100` 转小数。

**精确验证**：设佣金 0.03%、印花税 0.08%、过户费 0.0015% 跑真实回测，对 fills 逐行核对 —— 卖出行 `stamp_duty/amount = 0.000800`、`transfer_fee/amount = 0.000015`、`commission/amount = 0.000300`，三项费率精确生效；买入行无印花税，符合规则。

### 25.4 任务三：净值图改收益率口径

- 后端 `services/chartdata.py`：`nav_data` 输出改为 `series = df[col] - 1.0`（净值→累计收益率），序列中文名改为 组合收益率 / 基准收益率 / 超额收益率。
- 前端 `LineChart.jsx`：新增 `percentAxis` 开关 —— y 轴 `tickformat '.1%'`、hover 模板 `%{y:.2%}`；概览页图标题"组合 / 基准 / 超额收益率"、yLabel"累计收益率"。回撤、Alpha/Beta 贡献等收益类图同步启用百分比轴。

### 25.5 任务四：历史持仓页

**后端**（`routers/artifacts.py`）：

- `GET /api/runs/{run_id}/positions/latest?periods=2`：读 fills.parquet，过滤 `filled_quantity > 0`，取最新 N 个唯一 `fill_date`（倒序），每期返回 records（fill_date / asset_id / side / filled_quantity / fill_price / filled_amount / commission / stamp_duty / transfer_fee / explicit_cost / total_cost / status）与汇总（n_buy / n_sell / buy_amount / sell_amount / explicit_cost）。
- `GET /api/runs/{run_id}/positions/export`：全部成交记录导出 CSV，`utf-8-sig`（带 BOM，Excel 打开中文不乱码），`Content-Disposition: attachment; filename="{run_id}_all_fills.csv"`。

**前端**：

- `App.jsx` tabs 新增 `{ id: 'positions', label: '历史持仓' }`（持仓约束与报告之间）。
- `Dashboard.jsx` 新增 `case 'positions'`：每期一张表（标的 / 方向 / 成交股数 / 成交价 / 成交金额 / 佣金 / 印花税 / 过户费 / 费用合计），期标题带汇总（买入 N 笔 / 金额 · 卖出 N 笔 / 金额 · 显式费用）；右上角 `<a download>` 指向 export 端点，文案"⬇ 下载全部历史 (CSV)"。

### 25.6 Web 因子值上传与统一因子入口

**数据库**（`db.py`）：SCHEMA 新增 `imported_factors` 表（factor_id 主键、name、source、direction、file_path、n_dates、n_assets、date_start、date_end、coverage_ratio、content_hash、created_at）。

**上传 API**（`routers/factor_values.py`）：

- `POST /api/factor-values`（multipart：file + name + direction + source）：后缀必须 `.parquet`、direction ±1、factor_id 由名称清洗生成（`[^0-9A-Za-z_.\-] → _` 并追加短哈希防撞）。流程为**原子写入**：先写临时文件 → `inspect_factor_values` 体检 → `load_factor_values` 规范化 → 以规范化后帧重算 stats（保证注册元数据与落盘数据一致）→ `to_parquet` → `replace` 到最终路径 `data/factor_values/{factor_id}.parquet` → 注册 DB；任一步失败清理临时文件并返回 400。
- `GET /api/factor-values` 列表、`DELETE /api/factor-values/{factor_id}` 删除（文件 + DB 记录）。

**统一因子列表**：`GET /api/factors` 返回导入因子（前）+ 内置公式因子（后）合并列表，`FactorInfo` 新增 `kind: "demo" | "values"` 与区间/覆盖率元数据。

**回测接线**：`RunConfig` 新增 `factor_source: "demo" | "values"` 与 `factor_direction`。`services/runner.py` 在 values 源时查 `db.get_imported_factor`，把 `factor_values_path` 连同导入时声明的名称/来源**冻结进 run config**（保证 run 可复现；文件被删时显式 raise，由 `routers/runs.py` 转 400）。`engine.py` 的 values 分支用 `load_factor_values(..., data_version="web-import")` 构建 FactorFrame，方向缺省沿用导入时声明。

**前端**（`ConfigForm.jsx`）：因子下拉用 `<optgroup>` 分"导入因子值 (已算好)"/"内置公式因子"；选中导入因子时显示区间覆盖率 hint 与方向选择（+1/-1）、隐藏回看窗口，并把开始/结束日期自动 clamp 到因子区间；上传面板（文件 + 名称 + 方向 + 来源 + 已导入列表含删除）。

### 25.7 端到端验证证据

**CLI**：见 25.2.5（58 日 +11.06%，残差 0，39 产物）。

**Web 因子值回测**：界面上传参考包 `u5_factor`（导入为 `u5_factor_2a9abf845d88`，区间 2019-01-02 ~ 2024-04-09，覆盖率 80.0%），配置高费率（佣金 0.03% / 印花税 0.08% / 过户费 0.0015% / 滑点 12bps）月度调仓跑完：58 日 +10.94%（与 CLI 官方费率版本 +11.06% 的差异即费用提高所致，方向与量级合理）。

**浏览器可视化验证**（Playwright 无头 + DOM 断言，控制台零报错）：

| 验证点 | 实测 |
| --- | --- |
| 新建回测页 | 因子分组下拉、导入面板、已导入列表（u5_factor_2a9abf845d88 · gj_alpha80反向 · +1 · 2019-01-02~2024-04-09 · 80.0%）、费用四项（滑点12/佣金0.025/印花税留空/过户费留空）全部正常渲染 |
| 指标表格式 | 累计收益 `10.94%`；**Sharpe `1.886`（数值）**；**总成本 `567,960.56`（千分位金额）**；成本侵蚀比 `4.52%`；平均持股数 `92`（整数）—— 三列口径一致 |
| 收益率图 | 标题"组合 / 基准 / 超额收益率"，y 轴"累计收益率"，刻度 `−15.0% ~ 10.0%` 百分比 |
| 历史持仓页 | 最新两期 `2024-03-01 调仓` / `2024-02-01 调仓` 共 391 行明细，字段齐全（如 `000060.SZ 卖出 52,300 股 @4.328 · 226,369.96 元 · 佣金 67.91 · 印花税 181.10 · 过户费 3.40`） |
| CSV 下载 | `GET .../positions/export` 返回 200，`content-disposition: attachment`，`utf-8-sig` BOM 头，397 行（1 表头 + 396 成交） |

### 25.8 关键文件变更

**qbt 核心（两系统同步）**：

| 文件 | 变更 |
| --- | --- |
| `src/qbt/factors/values.py` | 新增：外部因子值加载/体检/构建 FactorFrame |
| `src/qbt/factors/__init__.py` | 导出三个新 API |
| `src/qbt/cli/main.py` | `--factor-values / --factor-id / --factor-direction / --factor-desc`，互斥校验 |
| `tests/unit/test_factor_values.py` | 新增 13 测试（仅主系统） |
| `quant_backtest_system_clean/SHA256SUMS`、`DELIVERY_MANIFEST.md` | 79 文件重签、增量章节 |

**Web 后端**：`db.py`（imported_factors 表）、`models.py`（FactorInfo.kind、RunConfig 四新字段）、`engine.py`（values 分支 + `_build_cost_config`）、`services/runner.py`（冻结导入路径）、`services/chartdata.py`（收益率口径）、`routers/factor_values.py`（新增）、`routers/artifacts.py`（positions 两端点）、`routers/runs.py`（ValueError→400）、`routers/meta.py`（合并因子列表）、`main.py`（注册路由）。

**Web 前端**：`api.js`（上传/删除/最新持仓/导出 URL）、`ConfigForm.jsx`（分组/上传/方向/费用四项/日期 clamp）、`Dashboard.jsx`（METRIC_TYPES、收益率图、历史持仓页）、`App.jsx`（页签）、`charts/LineChart.jsx`（percentAxis）、`styles.css`（上传面板等样式）。

### 25.9 工程经验

1. **格式化要跟着指标语义走**：展示层 bug 的根因是"一列一个格式函数"而不是"一个指标一个格式函数"。任何指标表的正确抽象是按指标 key 声明类型（pct/num/money/int），三列共用。
2. **可选参数用"不传"表达"用默认"**：费率可调的实现里，留空不传 schedule 比"传一个官方默认值"更好 —— 后者会把时变档位冻结成单档，悄悄改变回测语义。
3. **导入数据走"临时文件→体检→原子替换"**：规范化后的数据才落盘、注册元数据基于落盘帧重算，保证 DB 里登记的形状/覆盖率与文件永远一致。
4. **多副本同步先 diff 再最小改动**：clean 副本覆盖事故（25.2.6）的教训；交付副本的"交付特定默认值"必须在同步时显式保留。
5. **run 可复现要求冻结输入**：Web 的 values 回测把因子文件路径在提交时写进 config，而不是运行时再查列表 —— 文件删除后重放会显式失败而不是悄悄换数据。

本手册现在是独立技术文档：它描述需求、设计、代码行为、修复、测试和产物证据，不包含任何关于资料来源或阅读过程的说明。
